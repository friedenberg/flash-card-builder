{
  "devDependencies": {
    "eslint": "^7.8.1",
    "eslint-config-google": "^0.14.0"
  },
  "scripts": {
    "lint": "find . -iname '*.js' -a ! -ipath './node_modules/**' -a ! -ipath './_require.js' | xargs npx eslint --fix"
  }
}
